---
title: Unban
icon: lucide/code-2
---

# Unban

User unbanning commands for Discord moderation.

This module provides functionality to unban users from Discord servers,
with support for resolving users from ban lists using various identifiers.

## Classes

Discord cog for unbanning users from servers.

### Methods

```python
def __init__(self, bot: Bot) -> None
```

Initialize the Unban cog.

Parameters
----------
bot : Bot
    The bot instance to attach this cog to.

```python
async def resolve_user_from_ban_list(self, ctx: commands.Context[Bot], identifier: str) -> discord.User | None
```

Resolve a user from the ban list using username, ID, or partial info.

Parameters
----------
ctx : commands.Context[Bot]
    The context of the command.
identifier : str
    The username, ID, or partial identifier to resolve.

Returns
-------
discord.User | None
    The user if found, None otherwise.

*Decorated with: `hybrid_command`, `guild_only`, `requires_command_permission`*

```python
async def unban(self, ctx: commands.Context[Bot], username_or_id: str, reason: str | None, flags: UnbanFlags) -> None
```

Unban a user from the server.

Parameters
----------
ctx : commands.Context[Bot]
    The context object for the command.
username_or_id : str
    The username or ID of the user to unban.
reason : str | None
    The reason for the unban.
flags : UnbanFlags
    The flags for the command.

## Functions

```python
async def setup(bot: Bot) -> None
```

Set up the Unban cog.

Parameters
----------
bot : Bot
    The bot instance to add the cog to.
