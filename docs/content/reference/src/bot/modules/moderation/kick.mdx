---
title: Kick
icon: lucide/code-2
---

# Kick

Kick moderation command for Bot Bot.

This module provides the kick command functionality, allowing server
moderators to kick users from the server.

## Classes

Kick command cog for moderating server members.

### Methods

```python
def __init__(self, bot: Bot) -> None
```

Initialize the Kick cog.

Parameters
----------
bot : Bot
    The bot instance to initialize the cog with.

*Decorated with: `hybrid_command`, `guild_only`, `requires_command_permission`*

```python
async def kick(self, ctx: commands.Context[Bot], member: discord.Member, flags: KickFlags) -> None
```

Kick a member from the server.

Parameters
----------
ctx : commands.Context[Bot]
    The context in which the command is being invoked.
member : discord.Member
    The member to kick.
flags : KickFlags
    The flags for the command. (reason: str, silent: bool)

## Functions

```python
async def setup(bot: Bot) -> None
```

Set up the Kick cog.

Parameters
----------
bot : Bot
    The bot instance to add the cog to.
