---
title: Warn
icon: lucide/code-2
---

# Warn

User warning commands for Discord moderation.

This module provides functionality to issue warnings to Discord server members,
with automatic case tracking and notification systems.

## Classes

Discord cog for issuing warnings to server members.

### Methods

```python
def __init__(self, bot: Bot) -> None
```

Initialize the Warn cog.

Parameters
----------
bot : Bot
    The bot instance to attach this cog to.

*Decorated with: `hybrid_command`, `guild_only`, `requires_command_permission`*

```python
async def warn(self, ctx: commands.Context[Bot], member: discord.Member, flags: WarnFlags) -> None
```

Warn a member from the server.

Parameters
----------
ctx : commands.Context[Bot]
    The context in which the command is being invoked.
member : discord.Member
    The member to warn.
flags : WarnFlags
    The flags for the command. (reason: str, silent: bool)

## Functions

```python
async def setup(bot: Bot) -> None
```

Set up the Warn cog.

Parameters
----------
bot : Bot
    The bot instance to add the cog to.
