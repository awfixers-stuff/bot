# Configuration for actionlint (static analysis of GitHub Actions workflows)
# https://github.com/rhysd/actionlint/blob/main/docs/config.md

self-hosted-runner:
  # Custom runner labels used by this repo
  labels:
    - blacksmith-4vcpu-ubuntu-2404
